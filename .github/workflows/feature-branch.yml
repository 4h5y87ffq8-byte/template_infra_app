name: Feature Branch Quality Checks

on:
  push:
    branches:
      - 'feature/**'
  pull_request:
    branches:
      - 'feature/**'

jobs:
  quality-checks:
    uses: 4h5y87ffq8-byte/a5x-reusable-workflows/.github/workflows/feature-java.yml@main
    with:
      java-version: '17'
    secrets: inherit
