name: Feature Branch Quality Checks

on:
  push:
    branches:
      - 'feature/**'
  pull_request:
    branches:
      - 'feature/**'

jobs:
  workflow-java-feature:
    uses: 4h5y87ffq8-byte/demo-reusable-workflows/.github/workflows/feature-java.yml@main
    secrets: inherit
